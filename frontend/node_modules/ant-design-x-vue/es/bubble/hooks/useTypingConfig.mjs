import { computed as e, toValue as o } from "vue";
function i(n) {
  const r = e(() => !!o(n)), f = {
    step: 1,
    interval: 50,
    // set default suffix is empty
    suffix: null
  }, t = e(() => {
    const u = o(n);
    return {
      ...f,
      ...typeof u == "object" ? u : {}
    };
  });
  return [
    r,
    e(() => t.value.step),
    e(() => t.value.interval),
    e(() => t.value.suffix)
  ];
}
export {
  i as default
};
